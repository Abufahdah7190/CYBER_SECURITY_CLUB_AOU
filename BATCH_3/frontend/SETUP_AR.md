# تشغيل CyberClub محليًا

> هذه الحزمة مجهزة للتطوير المحلي. لا تستخدم قيم ملف `server/.env` الحالية في بيئة الإنتاج؛ يجب استبدال مفاتيح JWT وإنشاء إعدادات بريد وقاعدة بيانات إنتاجية.

## المتطلبات

يحتاج المشروع إلى **Node.js 18 أو أحدث** وnpm. لتشغيل التسجيل وتسجيل الدخول يلزم PostgreSQL. أسهل طريقة هي تثبيت Docker Desktop، لأن المشروع يحتوي على ملف `docker-compose.yml` جاهز.

## التشغيل التلقائي

من داخل مجلد المشروع:

```bash
chmod +x setup.sh
./setup.sh
cd server
npm start
```

بعد ذلك افتح [http://localhost:3000](http://localhost:3000). ستجد تبويب **تسجيل الدخول / التسجيل** ضمن التبويبات الرئيسية.

يفتح Docker قاعدة بيانات باسم `cyberclub` بالمستخدم `postgres` وكلمة المرور `devpassword` على المنفذ `5432`. يقوم السكربت بتثبيت الحزم وتشغيل ترحيلات قاعدة البيانات تلقائيًا.

## في حال عدم استخدام Docker

ثبّت PostgreSQL وأنشئ قاعدة بيانات باسم `cyberclub`، ثم تأكد من أن السطر التالي في `server/.env` يطابق إعداداتك:

```env
DATABASE_URL=postgresql://postgres:كلمة_المرور@localhost:5432/cyberclub
```

بعد تشغيل PostgreSQL نفّذ:

```bash
cd server
npm install
npm run migrate
npm start
```

## بوابة تسجيل الدخول

عند فتح الموقع سيظهر للزائر قسم **تسجيل الدخول / التسجيل** أولًا، وتُخفى بقية الأقسام حتى ينجح تسجيل الدخول أو إنشاء الحساب. بعد المصادقة تُفتح أقسام الموقع تلقائيًا. عند تسجيل الخروج تعود البوابة وتُحجب بقية الأقسام مرة أخرى.

## التحقق عبر البريد واستعادة كلمة المرور

النسخة الحالية لا تطلب رمز تحقق عند التسجيل؛ التسجيل ينشئ الحساب ويسجل الدخول مباشرة. استعادة كلمة المرور تُرسَل عبر **Nodemailer + Gmail SMTP** — مجاني بالكامل ويرسل لأي مزوّد بريد (Gmail, Outlook, Yahoo...) بدون حاجة لتوثيق نطاق. يلزم فقط حساب Gmail مع App Password (راجع `server/README.md` قسم "البريد الإلكتروني" للخطوات).

في التطوير، إذا تركت `EMAIL_USER` و`EMAIL_PASS` فارغين، سيطبع الخادم رسالة البريد ورابط الاستعادة في نافذة الطرفية بدل إرسالها فعليًا. في الإنتاج أضف بيانات حساب Gmail الفعلية. وتستخدم استعادة كلمة المرور المسار الموجود في الخادم لإرسال رابط إعادة التعيين.

## المعاينة التصميمية

لمشاهدة التصميم دون تشغيل Backend، افتح الملف `auth-preview.html` مباشرة في المتصفح. هذه الصفحة تفاعلية للعرض فقط ولا ترسل بيانات.

## ملاحظات مهمة

يفضل فتح الموقع من `http://localhost:3000` بدل النقر على `index.html` مباشرة؛ لأن واجهة المصادقة ترسل الطلبات إلى `/api/auth` وتحتاج إلى ملفات تعريف الارتباط التي يديرها الخادم. كما أن التسجيل مقيد افتراضيًا بالبريد الجامعي `@aou.edu.sa` وبصيغة رقم الطالب وفق إعدادات الخادم.
