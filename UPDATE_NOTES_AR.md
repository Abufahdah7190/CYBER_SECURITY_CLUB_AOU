# ملاحظات تحديث المنصة

## تحديث نظام الشهادات (تخصيص + ثيمات + QR)

| المجال | التحديث المنفذ |
| --- | --- |
| تخصيص الشهادة | عند إكمال آخر درس في الدورة (أو عند إعادة الإصدار من ملف الطالب) تظهر نافذة تفاعلية لاختيار **لغة الشهادة** (عربي/إنجليزي) و**ثيم اللون** (فاتح/أبيض أو داكن/سيبراني) قبل إصدارها. |
| التصميم | تصميمان احترافيان جديدان مبنيان على شعار النادي الفعلي (`assets/branding/cyberclub-logo.png`) بنفس هوية النماذج المرجعية: فاتح بإطار متدرج بنفسجي-أزرق وخطوط دارات كهربائية في الزوايا، وداكن بخلفية شبه سوداء وخطوط نيون سماوية/زرقاء كثيفة. |
| البيانات الديناميكية | اسم الطالب (من الحساب)، اسم الدورة، تاريخ الإصدار، ورمز الشهادة الفريد (Certificate ID) جميعها مضمّنة تلقائيًا في كلا الثيمين. |
| رمز QR والتحقق | يُولَّد رمز QR فريد لكل شهادة ويشير إلى `certificate-verify.html?code=...`. صفحة التحقق العامة تعرض الآن صورة الشهادة الفعلية بالإضافة إلى بيانات التحقق. |
| نقطة نهاية جديدة | `GET /api/learning/certificates/:certificateCode/image` (عامة، بدون تسجيل دخول) تُعيد ملف SVG الخاص بالشهادة بحسب الثيم المخزَّن — تُستخدم في صفحة الدورة، ملف الطالب، وصفحة التحقق. |
| قاعدة البيانات | عمود جديد `theme` في جدول `student_course_certificates` (migration `005_certificate_theme.sql`)، بقيمة افتراضية `light` وقيد يقبل `light`/`dark` فقط. |
| الواجهات المعدَّلة | `js/course.js` (نافذة الاختيار عند الإكمال + معاينة الشهادة الفعلية)، `js/profile.js` (زر "تخصيص" لإعادة الإصدار بلغة/ثيم مختلفين + صور مصغّرة حقيقية للشهادات)، `js/certificate-verify.js` (عرض صورة الشهادة). |


تم تحديث المنصة لتقييد التسجيل واستعادة كلمة المرور بالبريد الجامعي الرسمي فقط، ولإصدار الشهادات وإرسالها تلقائيًا عند إكمال الدورة.

| المجال | التحديث المنفذ |
| --- | --- |
| التسجيل والاستعادة | تُقبل فقط العناوين المنتهية بـ `@aou.edu.sa` أو `@aou.edu`. تعرض الواجهة المثال `student@aou.edu.sa`، وتطبق نمط تحقق قبل الإرسال. يعيد الخادم للبريد غير المسموح الرسالة: `الموقع متاح فقط لطلاب الجامعة العربية المفتوحة بالبريد الجامعي الرسمي`. |
| الشهادات | تُقرأ أسماء الطالبين الأول والأخير من الحساب عند الإصدار، ثم تُخزّن في الشهادة وتظهر في المعاينة والتحقق العام. |
| ملف الشهادة | ينشئ الخادم ملف SVG متجهيًا بدقة `1600×1131` يتضمن الاسم العربي، اسم الدورة، الرقم التسلسلي، ورمز QR القابل للتحقق. يُرفق الملف تلقائيًا برسالة التهنئة. |
| الإرسال التلقائي | عند وصول تقدم الدورة إلى 100%، تُنشأ الشهادة مرة واحدة ثم يُجدول إرسال البريد في الخلفية؛ لا تنتظر الواجهة إنشاء المرفق أو اتصال البريد. |
| البريد والمهلات | يستخدم Gmail SMTP عبر `smtp.gmail.com:465` مع `secure: true` ومهلات اتصال/تحية/مقبس مقدارها 5 ثوانٍ. كما توجد مهلة نهائية قابلة للضبط عبر `EMAIL_SEND_TIMEOUT_MS`. |
| حالة الواجهة | أزيل طلب الإصدار المكرر في صفحة الدورة. تعود حالة الزر فور انتهاء حفظ التقدم، وتنفّذ استعادة كلمة المرور إرسال البريد خارج استجابة الطلب. |

## إعداد النشر

اضبط متغيرات البيئة في خدمة الاستضافة، من دون إضافتها إلى المستودع:

```dotenv
NODE_ENV=production
DATABASE_URL=...
JWT_ACCESS_SECRET=...
JWT_REFRESH_SECRET=...
FRONTEND_URL=https://YOUR-DOMAIN
EMAIL_USER=club-mail@example.com
EMAIL_PASS=GOOGLE_APP_PASSWORD
EMAIL_FROM=
EMAIL_FROM_NAME=نادي الأمن السيبراني - AOU
EMAIL_SEND_TIMEOUT_MS=8000
```

> يجب أن يكون `EMAIL_PASS` كلمة مرور تطبيق Google، وليس كلمة مرور حساب Gmail العادية. أبقِ `EMAIL_FROM` فارغًا إذا لم يكن هناك عنوان إرسال مفوّض؛ سيُستخدم `EMAIL_USER` تلقائيًا.

## التحقق المنفذ

تم فحص صياغة ملفات JavaScript المحدثة، وتشغيل اختبار محلي لقواعد النطاقين الجامعيين، ورسالة رفض واجهات التسجيل والاستعادة، وتنسيق الاسم الكامل، وتوليد مرفق شهادة يتضمن الاسم العربي وQR، ودعم المرفقات في مسار البريد التطويري.
